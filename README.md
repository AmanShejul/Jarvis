# JARVIS

Personal desktop AI assistant interface. The frontend lives in [`frontend/`](./frontend). A Python backend can be added later behind the service functions in `frontend/src/services/api.js`.

## Start the frontend

```bash
cd frontend
npm install
npm run dev
```
