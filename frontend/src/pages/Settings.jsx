import { Bell, Keyboard, Palette, ShieldCheck, Wifi } from 'lucide-react'
import PageFrame from './PageFrame'
import { useState } from 'react'

const settingRows = [['Ambient HUD glow', 'Keep the interface softly illuminated', Palette], ['Desktop notifications', 'Allow JARVIS to surface task reminders', Bell], ['Secure local bridge', 'Require authorization before actions', ShieldCheck], ['Network awareness', 'Show connection state in the header', Wifi], ['Keyboard shortcuts', 'Enable command deck hotkeys', Keyboard]]
export default function Settings() { const [enabled, setEnabled] = useState([true, true, true, false, true]); return <PageFrame eyebrow="CONFIGURATION // USER PREFERENCES" title="Settings" description="Tune your command deck. Backend permissions can be added here later."><div className="settings-list">{settingRows.map(([title, detail, Icon], index) => <div className="setting-row" key={title}><span className="setting-icon"><Icon size={17} /></span><div><strong>{title}</strong><p>{detail}</p></div><button className={`toggle ${enabled[index] ? 'on' : ''}`} onClick={() => setEnabled((current) => current.map((value, row) => row === index ? !value : value))} aria-label={`Toggle ${title}`}><i /></button></div>)}</div><div className="settings-note"><ShieldCheck size={18} /><div><strong>Local-first by design</strong><p>JARVIS is currently running in presentation mode. No commands leave this device.</p></div></div></PageFrame> }
